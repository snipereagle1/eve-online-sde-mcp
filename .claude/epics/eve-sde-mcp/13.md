---
name: MCP Test Harness
status: open
created: 2026-05-10T19:46:54Z
updated: 2026-05-10T19:46:54Z
github: https://github.com/snipereagle1/eve-online-sde-mcp/issues/13
depends_on: [12]
parallel: true
conflicts_with: []
---

# Task: MCP Test Harness

## Description

Wire up `mcp_tester` (or equivalent) to exercise all 21 MCP tool handlers end-to-end against fixture data. No network calls; no live SDE download.

## Acceptance Criteria

- [ ] `mcp_tester` crate added as a `[dev-dependency]` in `Cargo.toml`
- [ ] All 21 tool handlers tested via MCP protocol (tool-call → response) using fixture data from task 12
- [ ] BFS route test: Jita → Perimeter (1 jump), unreachable system → error response
- [ ] Each test asserts on response shape (correct fields present, no unexpected keys)
- [ ] `cargo test` runs fully offline — no network, no live SDE download
- [ ] Tests are named clearly enough to identify which handler failed

## Technical Details

Spin up `SdeMcpServer` in-process pointing at `tests/fixtures/sde/`. Use `mcp_tester` client helpers to send JSON-RPC tool calls and assert on responses. Keep tests table-driven where handlers share the same input/output shape.

## Effort Estimate

- Size: M
