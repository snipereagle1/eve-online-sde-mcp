---
name: Tests, GitHub Actions & MCPB Bundle
status: closed
created: 2026-05-09T22:30:11Z
updated: 2026-05-10T19:46:54Z
github: https://github.com/snipereagle1/eve-online-sde-mcp/issues/11
depends_on: [7, 8, 9, 10]
parallel: false
conflicts_with: []
---

# Task: Tests, GitHub Actions & MCPB Bundle

## Description

Full test fixture suite covering all handlers. Three GitHub Actions workflows (CI, release, SDE bump). MCPB bundle manifest + release packaging.

## Acceptance Criteria

### Test Fixtures
- [ ] `tests/fixtures/sde/` contains minimal JSONL excerpts for all 16 files (10–20 records each)
- [ ] Fixture includes: Tritanium (type 34), Jita (system 30000142), Ferox blueprint, Tritanium market group chain, Caldari Navy faction, a skin record, dogma attributes
- [ ] All 20+ tool handlers tested against fixture data (no network calls)
- [ ] Build-number-parse unit test (URL → u64)
- [ ] `fetch_by_id` / `search_by_name` unit tests
- [ ] BFS route test: Jita→Perimeter (1 jump), unreachable → error
- [ ] `cargo test` passes fully offline

### CI Workflow (`.github/workflows/ci.yml`)
- [ ] Triggers: push + PR on `main`
- [ ] Steps: `cargo fmt --check`, `cargo clippy -- -D warnings`, `cargo test`, `cargo build --release`
- [ ] Uses fixture data — no network calls in test step
- [ ] Pinned to `PINNED_BUILD = 3333874`

### Release Workflow (`.github/workflows/release.yml`)
- [ ] Trigger: push of `v*` tag
- [ ] Parallel matrix: `x86_64-unknown-linux-gnu`, `x86_64-pc-windows-msvc`, `aarch64-apple-darwin`
- [ ] Assemble MCPB: collect 3 binaries → `server/` dir → `manifest.json` with tag version → `mcpb pack`
- [ ] GitHub Release: attach 3 platform binaries + `eve-sde-mcp.mcpb`, auto-changelog

### SDE Bump Workflow (`.github/workflows/sde-bump.yml`)
- [ ] Schedule: Mondays 06:00 UTC (`cron: '0 6 * * 1'`)
- [ ] HEAD redirect → parse new build number
- [ ] Compare against `PINNED_BUILD` in `src/sde_version.rs`
- [ ] If newer: sed-update constant, open PR titled `chore: bump CI SDE fixture to {build}`, labels `automated` + `sde-bump`
- [ ] Permissions: `contents: write`, `pull-requests: write`

### MCPB Manifest (`manifest.json`)
- [ ] Schema matches PRD §7.2 exactly
- [ ] Variables: `SDE_DATA_DIR` (optional), `SDE_LANGUAGE` (optional)
- [ ] Platform entrypoints: linux/windows/macos

## Technical Details

Cross-compile for macOS ARM from Linux CI: use `cross` crate or GitHub's macos-latest runner. Recommend macos-latest runner for ARM to avoid cross-compilation complexity.

`mcpb pack` command: verify `mcpb` is available in PATH on CI runners or install via release artifact download step.

Fixture JSONL format: each line must be valid JSON matching real SDE schema. Extract real records from PINNED_BUILD SDE for accuracy.

## Dependencies

Tasks 006, 007, 008, 009 (all tool handlers complete).

## Effort Estimate

- Size: L

## Definition of Done

- [ ] `cargo test` passes offline
- [ ] CI workflow runs green on GitHub
- [ ] Release workflow produces valid MCPB on tag push
- [ ] SDE bump workflow tested with dry-run or mocked HEAD response
- [ ] `cargo clippy -- -D warnings` clean
