---
name: Project Scaffold
status: open
created: 2026-05-09T22:30:11Z
updated: 2026-05-09T22:39:22Z
github: https://github.com/snipereagle1/eve-online-sde-mcp/issues/2
depends_on: []
parallel: false
conflicts_with: []
---

# Task: Project Scaffold

## Description

Init Rust binary crate. Wire all dependencies. Set up module structure. No logic yet — just compiles clean.

## Acceptance Criteria

- [ ] `cargo new --bin eve-sde-mcp` or equivalent workspace init
- [ ] `Cargo.toml` has all crates: rmcp, serde, serde_json, reqwest (blocking), zip, tokio (full), tracing, tracing-subscriber, anyhow, clap (derive), sha2, indicatif
- [ ] Module stubs created: `main.rs`, `config.rs`, `store.rs`, `download.rs`, `scan.rs`, `tools/mod.rs`
- [ ] `cargo build` succeeds with no errors
- [ ] `cargo clippy -- -D warnings` passes
- [ ] `src/sde_version.rs` with `pub const PINNED_BUILD: u64 = 3333874;`
- [ ] `.gitignore` covers `target/`, `*.mcpb`, SDE data dirs

## Technical Details

```toml
[dependencies]
rmcp = { version = "...", features = ["server", "transport-io"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
reqwest = { version = "0.12", features = ["blocking"] }
zip = "2"
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
anyhow = "1"
clap = { version = "4", features = ["derive"] }
sha2 = "0.10"
indicatif = "0.17"
```

Logging init in `main`: stderr only, level from `RUST_LOG` env (default `warn`).

## Dependencies

None.

## Effort Estimate

- Size: S

## Definition of Done

- [ ] Code implemented
- [ ] `cargo build --release` succeeds
- [ ] `cargo clippy -- -D warnings` clean
